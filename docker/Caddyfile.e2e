https://gateway {
	tls internal

	@backend path /api/v1/* /health/*
	handle @backend {
		reverse_proxy api:4000
	}

	@objectStorage path /danangmap/*
	handle @objectStorage {
		reverse_proxy minio:9000
	}

	handle {
		reverse_proxy frontend:3000
	}
}
